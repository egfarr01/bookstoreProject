//package com.example.bookstoreProject.WishList;

//import java.util.List;

//import org.springframework.data.jpa.repository.JpaRepository;
//import org.springframework.stereotype.Repository;
//import org.springframework.data.jpa.repository.Query;

//public class WishListRepositoryextends JpaRepository<{Wishlist, Long>
    //@Quary(value - "SELECT * from wishlist p WHERE " +
    //"p.user_id LIKE :userid", nativeQuery = true)
    //List<WishList> searchListsByUser(Long userid);

    
//}